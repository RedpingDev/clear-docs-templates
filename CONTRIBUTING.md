# 기여 안내

한국어와 영어로 피드백을 받습니다. 문서를 읽다가 멈춘 지점이나 어떤 설명이 더 필요했는지 구체적으로 알려주세요.

## 피드백 보내기

[이슈 양식](https://github.com/RedpingDev/clear-docs-templates/issues/new/choose)에서 표시·도구 문제, 양식 개선 제안, 사용 질문 중 하나를 선택합니다. 문제가 있는 문서와 읽는 환경, 기대한 결과를 적고 필요하면 스크린샷을 첨부합니다.

개선 제안에는 바뀐 형식이 어떤 독자의 이해를 돕는지도 적어주세요. 개인 경로나 비공개 프로젝트 내용이 담긴 스크린샷은 필요한 부분만 공유합니다.

## 수정과 PR

1. 저장소를 포크하고 `codex/설명` 형태의 작업 브랜치를 만듭니다.
2. [스타일 가이드](docs/style-guide.md)를 읽고 필요한 문서와 예시를 수정합니다.
3. 도식을 바꿨다면 `.mmd`와 PNG, 대체 텍스트·캡션·본문을 함께 갱신합니다.
4. 양식과 동작의 변경을 [변경 기록](CHANGELOG.md)의 Unreleased에 적습니다.
5. 변경 이유와 수행한 검사를 적어 `main`으로 PR을 보냅니다.

## 검사 실행

Node.js 24를 권장합니다. 처음에는 `npm ci`와 `npx playwright install chromium`을 실행하고, 한국어를 지원하는 글꼴을 준비합니다.

```bash
npm run check:scope
npm run check
npm run diagrams
npm run preview
```

단순 문구 수정은 문서 검사로 충분합니다. 도식·레이아웃·렌더링 도구를 바꿨다면 관련 이미지를 재생성하고 밝은·어두운 미리보기를 확인합니다.

CI는 Ubuntu에서 한국어 글꼴과 Chromium을 설치한 뒤 전체 검사를 실행합니다. 운영체제에 따른 PNG 해시 차이는 실패로 처리하지 않으며, CI가 변경한 PNG를 자동 커밋하지 않습니다.

## 라이선스와 작성 근거

기여물은 이 저장소의 [MIT 라이선스](LICENSE)로 제공할 수 있는 내용이어야 합니다. 다른 자료를 가져왔다면 출처와 필요한 고지를 함께 적습니다.

AI 초안도 사람이 검토합니다. 제공된 자료에 없는 동작이나 설정을 사실처럼 추가하지 않고, 예시는 [공통 자료](examples/source-notes.md)와 대조합니다.
